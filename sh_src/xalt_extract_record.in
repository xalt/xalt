#!/bin/bash
# -*- shell-script -*-
SCRIPT_NAME="${BASH_SOURCE[0]:-${(%):-%x}}"
SCRIPT_DIR=${SCRIPT_NAME%/*}

if [ $SCRIPT_DIR = "." ]; then
  SCRIPT_DIR=$PWD
fi  

LD_LIBRARY_PATH=${SCRIPT_DIR}/../lib64 ${SCRIPT_DIR}/../libexec/xalt_extract_record.x "$@"
