#!/bin/bash

DIRNAME=@path_to_dirname@
READLINK=@path_to_readlink@
MY_DIR=$($DIRNAME $($READLINK -f $0))

XALT_EXECUTABLE_TRACKING=no LD_LIBRARY_PATH=@cxx_ld_library_path@ $MY_DIR/../libexec/xalt_extract_record.x "$@"

